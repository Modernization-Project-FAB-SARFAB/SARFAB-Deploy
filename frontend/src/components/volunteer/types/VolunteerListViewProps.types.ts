interface VolunteerListViewProps {
    breadcrumb : { label: string; path?: string }[];
    columns: any;
    modalComponent?: JSX.Element[];
}

interface VolunteerCourseListViewProps {
    breadcrumb ?: { label: string; path?: string }[];
    columns: any;
    modalComponent?: JSX.Element[];
}